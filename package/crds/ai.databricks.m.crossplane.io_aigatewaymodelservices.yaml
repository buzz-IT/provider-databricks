---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: aigatewaymodelservices.ai.databricks.m.crossplane.io
spec:
  group: ai.databricks.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - databricks
    kind: AIGatewayModelService
    listKind: AIGatewayModelServiceList
    plural: aigatewaymodelservices
    singular: aigatewaymodelservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: AIGatewayModelService is the Schema for the AIGatewayModelServices
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AIGatewayModelServiceSpec defines the desired state of AIGatewayModelService
            properties:
              forProvider:
                properties:
                  comment:
                    description: User-provided description
                    type: string
                  config:
                    description: |-
                      Destinations, routing, rate limits, and payload logging configuration.
                      Required on Create. On Update, provide this field when update_mask
                      contains config or one of its subpaths
                    properties:
                      inferenceTable:
                        description: Inference table configuration for payload logging
                        properties:
                          parent:
                            description: |-
                              Name of the parent schema.
                              Format: schemas/{catalog}.{schema}.
                              Each {...} component is capped at 255 characters individually
                            type: string
                          tableNamePrefix:
                            description: |-
                              Prefix used to form the inference table's registered name. AI Gateway
                              appends _payload; for example, table_name_prefix = "orders" creates
                              orders_payload. If unset, the prefix defaults to the service name. Read
                              table from the response for the resulting resource name. After the
                              inference table is created, this field cannot be changed
                            type: string
                        type: object
                      rateLimits:
                        description: Rate limits applied to requests routed through
                          this model service
                        items:
                          properties:
                            key:
                              description: |-
                                Scope of the rate limit. Depending on this value, the limit applies to a
                                principal, the service as a whole, or each user by default. Possible values are: RATE_LIMIT_KEY_SERVICE, RATE_LIMIT_KEY_SERVICE_PRINCIPAL, RATE_LIMIT_KEY_USER, RATE_LIMIT_KEY_USER_DEFAULT, RATE_LIMIT_KEY_USER_GROUP
                              type: string
                            principal:
                              description: |-
                                Principal this limit applies to: user email, group name, or service
                                principal application ID. Required when key applies to a user, group, or
                                service principal; otherwise it must be unset
                              type: string
                            renewalPeriod:
                              description: 'Renewal period. Possible values are: RATE_LIMIT_RENEWAL_PERIOD_HOUR,
                                RATE_LIMIT_RENEWAL_PERIOD_MINUTE'
                              type: string
                            requests:
                              description: |-
                                Maximum requests allowed in one renewal period. Leave unset for no request
                                limit. Set to 0 to deny all requests
                              type: number
                            tokens:
                              description: |-
                                Maximum tokens allowed in one renewal period. Leave unset for no token
                                limit. Set to 0 to deny all requests
                              type: number
                          type: object
                        type: array
                      routing:
                        description: 'Routing configuration: destinations and fallback'
                        properties:
                          destinations:
                            description: Fallback destinations, tried in the listed
                              order. At most 5 are allowed
                            items:
                              properties:
                                destinationType:
                                  description: |-
                                    Backing-model category. Provide the matching type-specific configuration
                                    and leave the other type-specific configurations unset. Possible values are: DESTINATION_TYPE_EXTERNAL_FOUNDATION_MODEL, DESTINATION_TYPE_PAY_PER_TOKEN_FOUNDATION_MODEL, DESTINATION_TYPE_PROVISIONED_THROUGHPUT_FOUNDATION_MODEL
                                  type: string
                                externalModelConfig:
                                  description: Configuration for an external model
                                    reached through a model provider service
                                  properties:
                                    modelProviderService:
                                      description: |-
                                        Resource name of the governed ModelProviderService that owns provider
                                        auth and provider-specific configuration. The referenced
                                        ModelProviderService also carries the provider type, so this message
                                        does not surface it directly.
                                        Format: model-provider-services/{catalog}.{schema}.{model_provider_service}.
                                        Each {...} component is capped at 255 characters individually
                                      type: string
                                    target:
                                      description: |-
                                        Routing target for the destination: the provider-side model selected from
                                        the referenced ModelProviderService's targets catalog, plus the unified
                                        API types the platform should translate to/from at request time
                                      properties:
                                        model:
                                          description: |-
                                            Provider-side model identifier, such as gpt-5 or claude-opus-4-7.
                                            This identifies a model at the upstream provider; it is not a Unity
                                            Catalog model resource
                                          type: string
                                        nativeApiTypes:
                                          description: |-
                                            Provider-native API types supported by this model, such as
                                            openai/v1/chat/completions. At least one value is required. AI Gateway
                                            uses these values to translate requests and responses. At most 64 entries
                                            of 256 characters each are allowed
                                          items:
                                            type: string
                                          type: array
                                      type: object
                                  type: object
                                name:
                                  description: User-facing label for this destination,
                                    used in routing references
                                  type: string
                                payPerTokenConfig:
                                  description: Configuration for a pay-per-token Databricks
                                    foundation model
                                  properties:
                                    model:
                                      description: |-
                                        Provider-side model identifier, such as gpt-5 or claude-opus-4-7.
                                        This identifies a model at the upstream provider; it is not a Unity
                                        Catalog model resource
                                      type: string
                                  type: object
                                provisionedThroughputConfig:
                                  description: Configuration for a provisioned-throughput
                                    Databricks foundation model
                                  properties:
                                    modelServingEndpoint:
                                      description: |-
                                        Name of the backing Model Serving endpoint serving the provisioned-
                                        throughput foundation model, in the form serving-endpoints/{name}. The
                                        same Unity Catalog model can be served on multiple Model Serving endpoints
                                        with different throughput, regions, or configurations. The caller selects
                                        the endpoint to which this destination routes. The endpoint must exist at
                                        create time
                                      type: string
                                  type: object
                                trafficPercentage:
                                  description: |-
                                    Percentage of primary traffic sent to this destination, from 0 to 100.
                                    Required when there is more than one primary destination, in which case the
                                    primary percentages must sum to 100; a single primary destination receives
                                    all traffic. Fallback destinations are ordered and do not use this field
                                  type: number
                              type: object
                            type: array
                          fallback:
                            description: |-
                              Fallback routing applied after a primary destination fails. Fallback
                              destinations are tried in the listed order
                            properties:
                              destinations:
                                description: Fallback destinations, tried in the listed
                                  order. At most 5 are allowed
                                items:
                                  properties:
                                    destinationType:
                                      description: |-
                                        Backing-model category. Provide the matching type-specific configuration
                                        and leave the other type-specific configurations unset. Possible values are: DESTINATION_TYPE_EXTERNAL_FOUNDATION_MODEL, DESTINATION_TYPE_PAY_PER_TOKEN_FOUNDATION_MODEL, DESTINATION_TYPE_PROVISIONED_THROUGHPUT_FOUNDATION_MODEL
                                      type: string
                                    externalModelConfig:
                                      description: Configuration for an external model
                                        reached through a model provider service
                                      properties:
                                        modelProviderService:
                                          description: |-
                                            Resource name of the governed ModelProviderService that owns provider
                                            auth and provider-specific configuration. The referenced
                                            ModelProviderService also carries the provider type, so this message
                                            does not surface it directly.
                                            Format: model-provider-services/{catalog}.{schema}.{model_provider_service}.
                                            Each {...} component is capped at 255 characters individually
                                          type: string
                                        target:
                                          description: |-
                                            Routing target for the destination: the provider-side model selected from
                                            the referenced ModelProviderService's targets catalog, plus the unified
                                            API types the platform should translate to/from at request time
                                          properties:
                                            model:
                                              description: |-
                                                Provider-side model identifier, such as gpt-5 or claude-opus-4-7.
                                                This identifies a model at the upstream provider; it is not a Unity
                                                Catalog model resource
                                              type: string
                                            nativeApiTypes:
                                              description: |-
                                                Provider-native API types supported by this model, such as
                                                openai/v1/chat/completions. At least one value is required. AI Gateway
                                                uses these values to translate requests and responses. At most 64 entries
                                                of 256 characters each are allowed
                                              items:
                                                type: string
                                              type: array
                                          type: object
                                      type: object
                                    name:
                                      description: User-facing label for this destination,
                                        used in routing references
                                      type: string
                                    payPerTokenConfig:
                                      description: Configuration for a pay-per-token
                                        Databricks foundation model
                                      properties:
                                        model:
                                          description: |-
                                            Provider-side model identifier, such as gpt-5 or claude-opus-4-7.
                                            This identifies a model at the upstream provider; it is not a Unity
                                            Catalog model resource
                                          type: string
                                      type: object
                                    provisionedThroughputConfig:
                                      description: Configuration for a provisioned-throughput
                                        Databricks foundation model
                                      properties:
                                        modelServingEndpoint:
                                          description: |-
                                            Name of the backing Model Serving endpoint serving the provisioned-
                                            throughput foundation model, in the form serving-endpoints/{name}. The
                                            same Unity Catalog model can be served on multiple Model Serving endpoints
                                            with different throughput, regions, or configurations. The caller selects
                                            the endpoint to which this destination routes. The endpoint must exist at
                                            create time
                                          type: string
                                      type: object
                                    trafficPercentage:
                                      description: |-
                                        Percentage of primary traffic sent to this destination, from 0 to 100.
                                        Required when there is more than one primary destination, in which case the
                                        primary percentages must sum to 100; a single primary destination receives
                                        all traffic. Fallback destinations are ordered and do not use this field
                                      type: number
                                  type: object
                                type: array
                            type: object
                        type: object
                    type: object
                  modelServiceId:
                    description: Name for the model service, e.g. "my_model_service"
                    type: string
                  parent:
                    description: |-
                      Name of the parent schema.
                      Format: schemas/{catalog}.{schema}.
                      Each {...} component is capped at 255 characters individually
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  comment:
                    description: User-provided description
                    type: string
                  config:
                    description: |-
                      Destinations, routing, rate limits, and payload logging configuration.
                      Required on Create. On Update, provide this field when update_mask
                      contains config or one of its subpaths
                    properties:
                      inferenceTable:
                        description: Inference table configuration for payload logging
                        properties:
                          parent:
                            description: |-
                              Name of the parent schema.
                              Format: schemas/{catalog}.{schema}.
                              Each {...} component is capped at 255 characters individually
                            type: string
                          tableNamePrefix:
                            description: |-
                              Prefix used to form the inference table's registered name. AI Gateway
                              appends _payload; for example, table_name_prefix = "orders" creates
                              orders_payload. If unset, the prefix defaults to the service name. Read
                              table from the response for the resulting resource name. After the
                              inference table is created, this field cannot be changed
                            type: string
                        type: object
                      rateLimits:
                        description: Rate limits applied to requests routed through
                          this model service
                        items:
                          properties:
                            key:
                              description: |-
                                Scope of the rate limit. Depending on this value, the limit applies to a
                                principal, the service as a whole, or each user by default. Possible values are: RATE_LIMIT_KEY_SERVICE, RATE_LIMIT_KEY_SERVICE_PRINCIPAL, RATE_LIMIT_KEY_USER, RATE_LIMIT_KEY_USER_DEFAULT, RATE_LIMIT_KEY_USER_GROUP
                              type: string
                            principal:
                              description: |-
                                Principal this limit applies to: user email, group name, or service
                                principal application ID. Required when key applies to a user, group, or
                                service principal; otherwise it must be unset
                              type: string
                            renewalPeriod:
                              description: 'Renewal period. Possible values are: RATE_LIMIT_RENEWAL_PERIOD_HOUR,
                                RATE_LIMIT_RENEWAL_PERIOD_MINUTE'
                              type: string
                            requests:
                              description: |-
                                Maximum requests allowed in one renewal period. Leave unset for no request
                                limit. Set to 0 to deny all requests
                              type: number
                            tokens:
                              description: |-
                                Maximum tokens allowed in one renewal period. Leave unset for no token
                                limit. Set to 0 to deny all requests
                              type: number
                          type: object
                        type: array
                      routing:
                        description: 'Routing configuration: destinations and fallback'
                        properties:
                          destinations:
                            description: Fallback destinations, tried in the listed
                              order. At most 5 are allowed
                            items:
                              properties:
                                destinationType:
                                  description: |-
                                    Backing-model category. Provide the matching type-specific configuration
                                    and leave the other type-specific configurations unset. Possible values are: DESTINATION_TYPE_EXTERNAL_FOUNDATION_MODEL, DESTINATION_TYPE_PAY_PER_TOKEN_FOUNDATION_MODEL, DESTINATION_TYPE_PROVISIONED_THROUGHPUT_FOUNDATION_MODEL
                                  type: string
                                externalModelConfig:
                                  description: Configuration for an external model
                                    reached through a model provider service
                                  properties:
                                    modelProviderService:
                                      description: |-
                                        Resource name of the governed ModelProviderService that owns provider
                                        auth and provider-specific configuration. The referenced
                                        ModelProviderService also carries the provider type, so this message
                                        does not surface it directly.
                                        Format: model-provider-services/{catalog}.{schema}.{model_provider_service}.
                                        Each {...} component is capped at 255 characters individually
                                      type: string
                                    target:
                                      description: |-
                                        Routing target for the destination: the provider-side model selected from
                                        the referenced ModelProviderService's targets catalog, plus the unified
                                        API types the platform should translate to/from at request time
                                      properties:
                                        model:
                                          description: |-
                                            Provider-side model identifier, such as gpt-5 or claude-opus-4-7.
                                            This identifies a model at the upstream provider; it is not a Unity
                                            Catalog model resource
                                          type: string
                                        nativeApiTypes:
                                          description: |-
                                            Provider-native API types supported by this model, such as
                                            openai/v1/chat/completions. At least one value is required. AI Gateway
                                            uses these values to translate requests and responses. At most 64 entries
                                            of 256 characters each are allowed
                                          items:
                                            type: string
                                          type: array
                                      type: object
                                  type: object
                                name:
                                  description: User-facing label for this destination,
                                    used in routing references
                                  type: string
                                payPerTokenConfig:
                                  description: Configuration for a pay-per-token Databricks
                                    foundation model
                                  properties:
                                    model:
                                      description: |-
                                        Provider-side model identifier, such as gpt-5 or claude-opus-4-7.
                                        This identifies a model at the upstream provider; it is not a Unity
                                        Catalog model resource
                                      type: string
                                  type: object
                                provisionedThroughputConfig:
                                  description: Configuration for a provisioned-throughput
                                    Databricks foundation model
                                  properties:
                                    modelServingEndpoint:
                                      description: |-
                                        Name of the backing Model Serving endpoint serving the provisioned-
                                        throughput foundation model, in the form serving-endpoints/{name}. The
                                        same Unity Catalog model can be served on multiple Model Serving endpoints
                                        with different throughput, regions, or configurations. The caller selects
                                        the endpoint to which this destination routes. The endpoint must exist at
                                        create time
                                      type: string
                                  type: object
                                trafficPercentage:
                                  description: |-
                                    Percentage of primary traffic sent to this destination, from 0 to 100.
                                    Required when there is more than one primary destination, in which case the
                                    primary percentages must sum to 100; a single primary destination receives
                                    all traffic. Fallback destinations are ordered and do not use this field
                                  type: number
                              type: object
                            type: array
                          fallback:
                            description: |-
                              Fallback routing applied after a primary destination fails. Fallback
                              destinations are tried in the listed order
                            properties:
                              destinations:
                                description: Fallback destinations, tried in the listed
                                  order. At most 5 are allowed
                                items:
                                  properties:
                                    destinationType:
                                      description: |-
                                        Backing-model category. Provide the matching type-specific configuration
                                        and leave the other type-specific configurations unset. Possible values are: DESTINATION_TYPE_EXTERNAL_FOUNDATION_MODEL, DESTINATION_TYPE_PAY_PER_TOKEN_FOUNDATION_MODEL, DESTINATION_TYPE_PROVISIONED_THROUGHPUT_FOUNDATION_MODEL
                                      type: string
                                    externalModelConfig:
                                      description: Configuration for an external model
                                        reached through a model provider service
                                      properties:
                                        modelProviderService:
                                          description: |-
                                            Resource name of the governed ModelProviderService that owns provider
                                            auth and provider-specific configuration. The referenced
                                            ModelProviderService also carries the provider type, so this message
                                            does not surface it directly.
                                            Format: model-provider-services/{catalog}.{schema}.{model_provider_service}.
                                            Each {...} component is capped at 255 characters individually
                                          type: string
                                        target:
                                          description: |-
                                            Routing target for the destination: the provider-side model selected from
                                            the referenced ModelProviderService's targets catalog, plus the unified
                                            API types the platform should translate to/from at request time
                                          properties:
                                            model:
                                              description: |-
                                                Provider-side model identifier, such as gpt-5 or claude-opus-4-7.
                                                This identifies a model at the upstream provider; it is not a Unity
                                                Catalog model resource
                                              type: string
                                            nativeApiTypes:
                                              description: |-
                                                Provider-native API types supported by this model, such as
                                                openai/v1/chat/completions. At least one value is required. AI Gateway
                                                uses these values to translate requests and responses. At most 64 entries
                                                of 256 characters each are allowed
                                              items:
                                                type: string
                                              type: array
                                          type: object
                                      type: object
                                    name:
                                      description: User-facing label for this destination,
                                        used in routing references
                                      type: string
                                    payPerTokenConfig:
                                      description: Configuration for a pay-per-token
                                        Databricks foundation model
                                      properties:
                                        model:
                                          description: |-
                                            Provider-side model identifier, such as gpt-5 or claude-opus-4-7.
                                            This identifies a model at the upstream provider; it is not a Unity
                                            Catalog model resource
                                          type: string
                                      type: object
                                    provisionedThroughputConfig:
                                      description: Configuration for a provisioned-throughput
                                        Databricks foundation model
                                      properties:
                                        modelServingEndpoint:
                                          description: |-
                                            Name of the backing Model Serving endpoint serving the provisioned-
                                            throughput foundation model, in the form serving-endpoints/{name}. The
                                            same Unity Catalog model can be served on multiple Model Serving endpoints
                                            with different throughput, regions, or configurations. The caller selects
                                            the endpoint to which this destination routes. The endpoint must exist at
                                            create time
                                          type: string
                                      type: object
                                    trafficPercentage:
                                      description: |-
                                        Percentage of primary traffic sent to this destination, from 0 to 100.
                                        Required when there is more than one primary destination, in which case the
                                        primary percentages must sum to 100; a single primary destination receives
                                        all traffic. Fallback destinations are ordered and do not use this field
                                      type: number
                                  type: object
                                type: array
                            type: object
                        type: object
                    type: object
                  modelServiceId:
                    description: Name for the model service, e.g. "my_model_service"
                    type: string
                  parent:
                    description: |-
                      Name of the parent schema.
                      Format: schemas/{catalog}.{schema}.
                      Each {...} component is capped at 255 characters individually
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.modelServiceId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.modelServiceId)
                || (has(self.initProvider) && has(self.initProvider.modelServiceId))'
            - message: spec.forProvider.parent is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.parent)
                || (has(self.initProvider) && has(self.initProvider.parent))'
          status:
            description: AIGatewayModelServiceStatus defines the observed state of
              AIGatewayModelService.
            properties:
              atProvider:
                properties:
                  comment:
                    description: User-provided description
                    type: string
                  config:
                    description: |-
                      Destinations, routing, rate limits, and payload logging configuration.
                      Required on Create. On Update, provide this field when update_mask
                      contains config or one of its subpaths
                    properties:
                      inferenceTable:
                        description: Inference table configuration for payload logging
                        properties:
                          isDeleted:
                            description: |-
                              Whether the referenced inference table has been deleted. The configuration
                              remains visible so you can identify the broken dependency. Payload logging
                              cannot continue until the table is restored or the configuration is updated
                            type: boolean
                          parent:
                            description: |-
                              Name of the parent schema.
                              Format: schemas/{catalog}.{schema}.
                              Each {...} component is capped at 255 characters individually
                            type: string
                          table:
                            description: |-
                              Resolved UC table for payload logs.
                              Format: tables/{catalog}.{schema}.{table}
                            type: string
                          tableNamePrefix:
                            description: |-
                              Prefix used to form the inference table's registered name. AI Gateway
                              appends _payload; for example, table_name_prefix = "orders" creates
                              orders_payload. If unset, the prefix defaults to the service name. Read
                              table from the response for the resulting resource name. After the
                              inference table is created, this field cannot be changed
                            type: string
                        type: object
                      rateLimits:
                        description: Rate limits applied to requests routed through
                          this model service
                        items:
                          properties:
                            key:
                              description: |-
                                Scope of the rate limit. Depending on this value, the limit applies to a
                                principal, the service as a whole, or each user by default. Possible values are: RATE_LIMIT_KEY_SERVICE, RATE_LIMIT_KEY_SERVICE_PRINCIPAL, RATE_LIMIT_KEY_USER, RATE_LIMIT_KEY_USER_DEFAULT, RATE_LIMIT_KEY_USER_GROUP
                              type: string
                            principal:
                              description: |-
                                Principal this limit applies to: user email, group name, or service
                                principal application ID. Required when key applies to a user, group, or
                                service principal; otherwise it must be unset
                              type: string
                            renewalPeriod:
                              description: 'Renewal period. Possible values are: RATE_LIMIT_RENEWAL_PERIOD_HOUR,
                                RATE_LIMIT_RENEWAL_PERIOD_MINUTE'
                              type: string
                            requests:
                              description: |-
                                Maximum requests allowed in one renewal period. Leave unset for no request
                                limit. Set to 0 to deny all requests
                              type: number
                            tokens:
                              description: |-
                                Maximum tokens allowed in one renewal period. Leave unset for no token
                                limit. Set to 0 to deny all requests
                              type: number
                          type: object
                        type: array
                      routing:
                        description: 'Routing configuration: destinations and fallback'
                        properties:
                          destinations:
                            description: Fallback destinations, tried in the listed
                              order. At most 5 are allowed
                            items:
                              properties:
                                destinationType:
                                  description: |-
                                    Backing-model category. Provide the matching type-specific configuration
                                    and leave the other type-specific configurations unset. Possible values are: DESTINATION_TYPE_EXTERNAL_FOUNDATION_MODEL, DESTINATION_TYPE_PAY_PER_TOKEN_FOUNDATION_MODEL, DESTINATION_TYPE_PROVISIONED_THROUGHPUT_FOUNDATION_MODEL
                                  type: string
                                externalModelConfig:
                                  description: Configuration for an external model
                                    reached through a model provider service
                                  properties:
                                    modelProviderService:
                                      description: |-
                                        Resource name of the governed ModelProviderService that owns provider
                                        auth and provider-specific configuration. The referenced
                                        ModelProviderService also carries the provider type, so this message
                                        does not surface it directly.
                                        Format: model-provider-services/{catalog}.{schema}.{model_provider_service}.
                                        Each {...} component is capped at 255 characters individually
                                      type: string
                                    target:
                                      description: |-
                                        Routing target for the destination: the provider-side model selected from
                                        the referenced ModelProviderService's targets catalog, plus the unified
                                        API types the platform should translate to/from at request time
                                      properties:
                                        model:
                                          description: |-
                                            Provider-side model identifier, such as gpt-5 or claude-opus-4-7.
                                            This identifies a model at the upstream provider; it is not a Unity
                                            Catalog model resource
                                          type: string
                                        nativeApiTypes:
                                          description: |-
                                            Provider-native API types supported by this model, such as
                                            openai/v1/chat/completions. At least one value is required. AI Gateway
                                            uses these values to translate requests and responses. At most 64 entries
                                            of 256 characters each are allowed
                                          items:
                                            type: string
                                          type: array
                                      type: object
                                  type: object
                                isDeleted:
                                  description: |-
                                    Whether the referenced inference table has been deleted. The configuration
                                    remains visible so you can identify the broken dependency. Payload logging
                                    cannot continue until the table is restored or the configuration is updated
                                  type: boolean
                                name:
                                  description: User-facing label for this destination,
                                    used in routing references
                                  type: string
                                payPerTokenConfig:
                                  description: Configuration for a pay-per-token Databricks
                                    foundation model
                                  properties:
                                    model:
                                      description: |-
                                        Provider-side model identifier, such as gpt-5 or claude-opus-4-7.
                                        This identifies a model at the upstream provider; it is not a Unity
                                        Catalog model resource
                                      type: string
                                  type: object
                                provisionedThroughputConfig:
                                  description: Configuration for a provisioned-throughput
                                    Databricks foundation model
                                  properties:
                                    model:
                                      description: |-
                                        Provider-side model identifier, such as gpt-5 or claude-opus-4-7.
                                        This identifies a model at the upstream provider; it is not a Unity
                                        Catalog model resource
                                      type: string
                                    modelServingEndpoint:
                                      description: |-
                                        Name of the backing Model Serving endpoint serving the provisioned-
                                        throughput foundation model, in the form serving-endpoints/{name}. The
                                        same Unity Catalog model can be served on multiple Model Serving endpoints
                                        with different throughput, regions, or configurations. The caller selects
                                        the endpoint to which this destination routes. The endpoint must exist at
                                        create time
                                      type: string
                                  type: object
                                trafficPercentage:
                                  description: |-
                                    Percentage of primary traffic sent to this destination, from 0 to 100.
                                    Required when there is more than one primary destination, in which case the
                                    primary percentages must sum to 100; a single primary destination receives
                                    all traffic. Fallback destinations are ordered and do not use this field
                                  type: number
                              type: object
                            type: array
                          fallback:
                            description: |-
                              Fallback routing applied after a primary destination fails. Fallback
                              destinations are tried in the listed order
                            properties:
                              destinations:
                                description: Fallback destinations, tried in the listed
                                  order. At most 5 are allowed
                                items:
                                  properties:
                                    destinationType:
                                      description: |-
                                        Backing-model category. Provide the matching type-specific configuration
                                        and leave the other type-specific configurations unset. Possible values are: DESTINATION_TYPE_EXTERNAL_FOUNDATION_MODEL, DESTINATION_TYPE_PAY_PER_TOKEN_FOUNDATION_MODEL, DESTINATION_TYPE_PROVISIONED_THROUGHPUT_FOUNDATION_MODEL
                                      type: string
                                    externalModelConfig:
                                      description: Configuration for an external model
                                        reached through a model provider service
                                      properties:
                                        modelProviderService:
                                          description: |-
                                            Resource name of the governed ModelProviderService that owns provider
                                            auth and provider-specific configuration. The referenced
                                            ModelProviderService also carries the provider type, so this message
                                            does not surface it directly.
                                            Format: model-provider-services/{catalog}.{schema}.{model_provider_service}.
                                            Each {...} component is capped at 255 characters individually
                                          type: string
                                        target:
                                          description: |-
                                            Routing target for the destination: the provider-side model selected from
                                            the referenced ModelProviderService's targets catalog, plus the unified
                                            API types the platform should translate to/from at request time
                                          properties:
                                            model:
                                              description: |-
                                                Provider-side model identifier, such as gpt-5 or claude-opus-4-7.
                                                This identifies a model at the upstream provider; it is not a Unity
                                                Catalog model resource
                                              type: string
                                            nativeApiTypes:
                                              description: |-
                                                Provider-native API types supported by this model, such as
                                                openai/v1/chat/completions. At least one value is required. AI Gateway
                                                uses these values to translate requests and responses. At most 64 entries
                                                of 256 characters each are allowed
                                              items:
                                                type: string
                                              type: array
                                          type: object
                                      type: object
                                    isDeleted:
                                      description: |-
                                        Whether the referenced inference table has been deleted. The configuration
                                        remains visible so you can identify the broken dependency. Payload logging
                                        cannot continue until the table is restored or the configuration is updated
                                      type: boolean
                                    name:
                                      description: User-facing label for this destination,
                                        used in routing references
                                      type: string
                                    payPerTokenConfig:
                                      description: Configuration for a pay-per-token
                                        Databricks foundation model
                                      properties:
                                        model:
                                          description: |-
                                            Provider-side model identifier, such as gpt-5 or claude-opus-4-7.
                                            This identifies a model at the upstream provider; it is not a Unity
                                            Catalog model resource
                                          type: string
                                      type: object
                                    provisionedThroughputConfig:
                                      description: Configuration for a provisioned-throughput
                                        Databricks foundation model
                                      properties:
                                        model:
                                          description: |-
                                            Provider-side model identifier, such as gpt-5 or claude-opus-4-7.
                                            This identifies a model at the upstream provider; it is not a Unity
                                            Catalog model resource
                                          type: string
                                        modelServingEndpoint:
                                          description: |-
                                            Name of the backing Model Serving endpoint serving the provisioned-
                                            throughput foundation model, in the form serving-endpoints/{name}. The
                                            same Unity Catalog model can be served on multiple Model Serving endpoints
                                            with different throughput, regions, or configurations. The caller selects
                                            the endpoint to which this destination routes. The endpoint must exist at
                                            create time
                                          type: string
                                      type: object
                                    trafficPercentage:
                                      description: |-
                                        Percentage of primary traffic sent to this destination, from 0 to 100.
                                        Required when there is more than one primary destination, in which case the
                                        primary percentages must sum to 100; a single primary destination receives
                                        all traffic. Fallback destinations are ordered and do not use this field
                                      type: number
                                  type: object
                                type: array
                            type: object
                        type: object
                    type: object
                  createTime:
                    description: Time the model service was created
                    type: string
                  createdBy:
                    description: Creator identity
                    type: string
                  effectiveOwner:
                    description: Owner of the model service
                    type: string
                  etag:
                    description: |-
                      Optimistic concurrency token returned on every read. To make an Update or
                      Delete conditional, pass the last-read value in that request's etag
                      field. In REST responses, this value is a base64 string; URL-encode it when
                      setting the etag query parameter
                    type: string
                  id:
                    type: string
                  metastoreId:
                    description: Metastore hosting the model service
                    type: string
                  modelServiceId:
                    description: Name for the model service, e.g. "my_model_service"
                    type: string
                  name:
                    description: User-facing label for this destination, used in routing
                      references
                    type: string
                  parent:
                    description: |-
                      Name of the parent schema.
                      Format: schemas/{catalog}.{schema}.
                      Each {...} component is capped at 255 characters individually
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  supportedApiTypes:
                    description: |-
                      API types supported across this service's destinations, such as
                      openai/v1/chat/completions, openai/v1/embeddings, and
                      mlflow/v1/chat/completions. Derived from the backing models and providers
                      at read time
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  updateTime:
                    description: Time the model service was last modified
                    type: string
                  updatedBy:
                    description: Identity of the last updater
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
