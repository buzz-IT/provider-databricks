---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: modelservingprovisionedthroughputs.serving.databricks.crossplane.io
spec:
  group: serving.databricks.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - databricks
    kind: ModelServingProvisionedThroughput
    listKind: ModelServingProvisionedThroughputList
    plural: modelservingprovisionedthroughputs
    singular: modelservingprovisionedthroughput
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ModelServingProvisionedThroughput is the Schema for the ModelServingProvisionedThroughputs
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ModelServingProvisionedThroughputSpec defines the desired
              state of ModelServingProvisionedThroughput
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  aiGateway:
                    description: 'A block with AI Gateway configuration for the serving
                      endpoint. Note: only external model endpoints are supported
                      as of now.'
                    properties:
                      fallbackConfig:
                        description: The model serving endpoint configuration.
                        properties:
                          enabled:
                            description: boolean flag specifying if usage tracking
                              is enabled.
                            type: boolean
                        type: object
                      guardrails:
                        description: 'Block with configuration for AI Guardrails to
                          prevent unwanted data and unsafe data in requests and responses.
                          Consists of the following attributes:'
                        properties:
                          input:
                            description: 'A block with configuration for input guardrail
                              filters:'
                            properties:
                              invalidKeywords:
                                description: List of invalid keywords. AI guardrail
                                  uses keyword or string matching to decide if the
                                  keyword exists in the request or response content.
                                items:
                                  type: string
                                type: array
                              pii:
                                description: 'Block with configuration for guardrail
                                  PII filter:'
                                properties:
                                  behavior:
                                    description: a string that describes the behavior
                                      for PII filter. Currently only BLOCK value is
                                      supported.
                                    type: string
                                type: object
                              safety:
                                description: the boolean flag that indicates whether
                                  the safety filter is enabled.
                                type: boolean
                              validTopics:
                                description: The list of allowed topics. Given a chat
                                  request, this guardrail flags the request if its
                                  topic is not in the allowed topics.
                                items:
                                  type: string
                                type: array
                            type: object
                          output:
                            description: A block with configuration for output guardrail
                              filters.  Has the same structure as input block.
                            properties:
                              invalidKeywords:
                                description: List of invalid keywords. AI guardrail
                                  uses keyword or string matching to decide if the
                                  keyword exists in the request or response content.
                                items:
                                  type: string
                                type: array
                              pii:
                                description: 'Block with configuration for guardrail
                                  PII filter:'
                                properties:
                                  behavior:
                                    description: a string that describes the behavior
                                      for PII filter. Currently only BLOCK value is
                                      supported.
                                    type: string
                                type: object
                              safety:
                                description: the boolean flag that indicates whether
                                  the safety filter is enabled.
                                type: boolean
                              validTopics:
                                description: The list of allowed topics. Given a chat
                                  request, this guardrail flags the request if its
                                  topic is not in the allowed topics.
                                items:
                                  type: string
                                type: array
                            type: object
                        type: object
                      inferenceTableConfig:
                        description: 'Block describing the configuration of usage
                          tracking. Consists of the following attributes:'
                        properties:
                          catalogName:
                            description: 'The name of the model serving endpoint.
                              This field is required and must be unique across a workspace.
                              An endpoint name can consist of alphanumeric characters,
                              dashes, and underscores. NOTE: Changing this name will
                              delete the existing endpoint and create a new endpoint
                              with the updated name.'
                            type: string
                          enabled:
                            description: boolean flag specifying if usage tracking
                              is enabled.
                            type: boolean
                          schemaName:
                            description: 'The name of the model serving endpoint.
                              This field is required and must be unique across a workspace.
                              An endpoint name can consist of alphanumeric characters,
                              dashes, and underscores. NOTE: Changing this name will
                              delete the existing endpoint and create a new endpoint
                              with the updated name.'
                            type: string
                          tableNamePrefix:
                            type: string
                        type: object
                      rateLimits:
                        description: Block describing rate limits for AI gateway.
                          For details see the description of rate_limits block above.
                        items:
                          properties:
                            calls:
                              type: number
                            key:
                              description: The key field for a tag.
                              type: string
                            principal:
                              type: string
                            renewalPeriod:
                              type: string
                            tokens:
                              type: number
                          type: object
                        type: array
                      usageTrackingConfig:
                        description: Block with configuration for payload logging
                          using inference tables. For details see the description
                          of auto_capture_config block above.
                        properties:
                          enabled:
                            description: boolean flag specifying if usage tracking
                              is enabled.
                            type: boolean
                        type: object
                    type: object
                  budgetPolicyId:
                    description: (Optiona) The Budget Policy ID set for this serving
                      endpoint.
                    type: string
                  config:
                    description: The model serving endpoint configuration.
                    properties:
                      servedEntities:
                        description: A list of served entities for the endpoint to
                          serve.
                        items:
                          properties:
                            burstScalingEnabled:
                              description: boolean flag specifying if usage tracking
                                is enabled.
                              type: boolean
                            entityName:
                              description: The full path of the UC model to be served,
                                given in the form of catalog_name.schema_name.model_name.
                              type: string
                            entityVersion:
                              description: The version of the model in UC to be served.
                              type: string
                            name:
                              description: The name of a served entity. It must be
                                unique across an endpoint. A served entity name can
                                consist of alphanumeric characters, dashes, and underscores.
                                If not specified for an external model, this field
                                will be created from the entity_name and entity_version
                              type: string
                            provisionedModelUnits:
                              description: The number of model units to be provisioned.
                              type: number
                          type: object
                        type: array
                      trafficConfig:
                        description: A single block represents the traffic split configuration
                          amongst the served models.
                        properties:
                          routes:
                            description: Each block represents a route that defines
                              traffic to each served entity. Each served_entity block
                              needs to have a corresponding routes block.
                            items:
                              properties:
                                servedEntityName:
                                  description: The name of the served entity this
                                    route configures traffic for. This needs to match
                                    the name of a served_entity block.
                                  type: string
                                servedModelName:
                                  description: 'The name of the model serving endpoint.
                                    This field is required and must be unique across
                                    a workspace. An endpoint name can consist of alphanumeric
                                    characters, dashes, and underscores. NOTE: Changing
                                    this name will delete the existing endpoint and
                                    create a new endpoint with the updated name.'
                                  type: string
                                trafficPercentage:
                                  description: The percentage of endpoint traffic
                                    to send to this route. It must be an integer between
                                    0 and 100 inclusive.
                                  type: number
                              type: object
                            type: array
                        type: object
                    type: object
                  emailNotifications:
                    description: A block with Email notification setting.
                    properties:
                      onUpdateFailure:
                        description: a list of email addresses to be notified when
                          an endpoint fails to update its configuration or state.
                        items:
                          type: string
                        type: array
                      onUpdateSuccess:
                        description: a list of email addresses to be notified when
                          an endpoint successfully updates its configuration or state.
                        items:
                          type: string
                        type: array
                    type: object
                  name:
                    description: 'The name of the model serving endpoint. This field
                      is required and must be unique across a workspace. An endpoint
                      name can consist of alphanumeric characters, dashes, and underscores.
                      NOTE: Changing this name will delete the existing endpoint and
                      create a new endpoint with the updated name.'
                    type: string
                  providerConfig:
                    description: 'Configure the provider for management through account
                      provider. This block consists of the following fields:'
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  tags:
                    description: Tags to be attached to the serving endpoint and automatically
                      propagated to billing logs.
                    items:
                      properties:
                        key:
                          description: The key field for a tag.
                          type: string
                        value:
                          description: The value field for a tag.
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  aiGateway:
                    description: 'A block with AI Gateway configuration for the serving
                      endpoint. Note: only external model endpoints are supported
                      as of now.'
                    properties:
                      fallbackConfig:
                        description: The model serving endpoint configuration.
                        properties:
                          enabled:
                            description: boolean flag specifying if usage tracking
                              is enabled.
                            type: boolean
                        type: object
                      guardrails:
                        description: 'Block with configuration for AI Guardrails to
                          prevent unwanted data and unsafe data in requests and responses.
                          Consists of the following attributes:'
                        properties:
                          input:
                            description: 'A block with configuration for input guardrail
                              filters:'
                            properties:
                              invalidKeywords:
                                description: List of invalid keywords. AI guardrail
                                  uses keyword or string matching to decide if the
                                  keyword exists in the request or response content.
                                items:
                                  type: string
                                type: array
                              pii:
                                description: 'Block with configuration for guardrail
                                  PII filter:'
                                properties:
                                  behavior:
                                    description: a string that describes the behavior
                                      for PII filter. Currently only BLOCK value is
                                      supported.
                                    type: string
                                type: object
                              safety:
                                description: the boolean flag that indicates whether
                                  the safety filter is enabled.
                                type: boolean
                              validTopics:
                                description: The list of allowed topics. Given a chat
                                  request, this guardrail flags the request if its
                                  topic is not in the allowed topics.
                                items:
                                  type: string
                                type: array
                            type: object
                          output:
                            description: A block with configuration for output guardrail
                              filters.  Has the same structure as input block.
                            properties:
                              invalidKeywords:
                                description: List of invalid keywords. AI guardrail
                                  uses keyword or string matching to decide if the
                                  keyword exists in the request or response content.
                                items:
                                  type: string
                                type: array
                              pii:
                                description: 'Block with configuration for guardrail
                                  PII filter:'
                                properties:
                                  behavior:
                                    description: a string that describes the behavior
                                      for PII filter. Currently only BLOCK value is
                                      supported.
                                    type: string
                                type: object
                              safety:
                                description: the boolean flag that indicates whether
                                  the safety filter is enabled.
                                type: boolean
                              validTopics:
                                description: The list of allowed topics. Given a chat
                                  request, this guardrail flags the request if its
                                  topic is not in the allowed topics.
                                items:
                                  type: string
                                type: array
                            type: object
                        type: object
                      inferenceTableConfig:
                        description: 'Block describing the configuration of usage
                          tracking. Consists of the following attributes:'
                        properties:
                          catalogName:
                            description: 'The name of the model serving endpoint.
                              This field is required and must be unique across a workspace.
                              An endpoint name can consist of alphanumeric characters,
                              dashes, and underscores. NOTE: Changing this name will
                              delete the existing endpoint and create a new endpoint
                              with the updated name.'
                            type: string
                          enabled:
                            description: boolean flag specifying if usage tracking
                              is enabled.
                            type: boolean
                          schemaName:
                            description: 'The name of the model serving endpoint.
                              This field is required and must be unique across a workspace.
                              An endpoint name can consist of alphanumeric characters,
                              dashes, and underscores. NOTE: Changing this name will
                              delete the existing endpoint and create a new endpoint
                              with the updated name.'
                            type: string
                          tableNamePrefix:
                            type: string
                        type: object
                      rateLimits:
                        description: Block describing rate limits for AI gateway.
                          For details see the description of rate_limits block above.
                        items:
                          properties:
                            calls:
                              type: number
                            key:
                              description: The key field for a tag.
                              type: string
                            principal:
                              type: string
                            renewalPeriod:
                              type: string
                            tokens:
                              type: number
                          type: object
                        type: array
                      usageTrackingConfig:
                        description: Block with configuration for payload logging
                          using inference tables. For details see the description
                          of auto_capture_config block above.
                        properties:
                          enabled:
                            description: boolean flag specifying if usage tracking
                              is enabled.
                            type: boolean
                        type: object
                    type: object
                  budgetPolicyId:
                    description: (Optiona) The Budget Policy ID set for this serving
                      endpoint.
                    type: string
                  config:
                    description: The model serving endpoint configuration.
                    properties:
                      servedEntities:
                        description: A list of served entities for the endpoint to
                          serve.
                        items:
                          properties:
                            burstScalingEnabled:
                              description: boolean flag specifying if usage tracking
                                is enabled.
                              type: boolean
                            entityName:
                              description: The full path of the UC model to be served,
                                given in the form of catalog_name.schema_name.model_name.
                              type: string
                            entityVersion:
                              description: The version of the model in UC to be served.
                              type: string
                            name:
                              description: The name of a served entity. It must be
                                unique across an endpoint. A served entity name can
                                consist of alphanumeric characters, dashes, and underscores.
                                If not specified for an external model, this field
                                will be created from the entity_name and entity_version
                              type: string
                            provisionedModelUnits:
                              description: The number of model units to be provisioned.
                              type: number
                          type: object
                        type: array
                      trafficConfig:
                        description: A single block represents the traffic split configuration
                          amongst the served models.
                        properties:
                          routes:
                            description: Each block represents a route that defines
                              traffic to each served entity. Each served_entity block
                              needs to have a corresponding routes block.
                            items:
                              properties:
                                servedEntityName:
                                  description: The name of the served entity this
                                    route configures traffic for. This needs to match
                                    the name of a served_entity block.
                                  type: string
                                servedModelName:
                                  description: 'The name of the model serving endpoint.
                                    This field is required and must be unique across
                                    a workspace. An endpoint name can consist of alphanumeric
                                    characters, dashes, and underscores. NOTE: Changing
                                    this name will delete the existing endpoint and
                                    create a new endpoint with the updated name.'
                                  type: string
                                trafficPercentage:
                                  description: The percentage of endpoint traffic
                                    to send to this route. It must be an integer between
                                    0 and 100 inclusive.
                                  type: number
                              type: object
                            type: array
                        type: object
                    type: object
                  emailNotifications:
                    description: A block with Email notification setting.
                    properties:
                      onUpdateFailure:
                        description: a list of email addresses to be notified when
                          an endpoint fails to update its configuration or state.
                        items:
                          type: string
                        type: array
                      onUpdateSuccess:
                        description: a list of email addresses to be notified when
                          an endpoint successfully updates its configuration or state.
                        items:
                          type: string
                        type: array
                    type: object
                  name:
                    description: 'The name of the model serving endpoint. This field
                      is required and must be unique across a workspace. An endpoint
                      name can consist of alphanumeric characters, dashes, and underscores.
                      NOTE: Changing this name will delete the existing endpoint and
                      create a new endpoint with the updated name.'
                    type: string
                  providerConfig:
                    description: 'Configure the provider for management through account
                      provider. This block consists of the following fields:'
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  tags:
                    description: Tags to be attached to the serving endpoint and automatically
                      propagated to billing logs.
                    items:
                      properties:
                        key:
                          description: The key field for a tag.
                          type: string
                        value:
                          description: The value field for a tag.
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.config is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.config)
                || (has(self.initProvider) && has(self.initProvider.config))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: ModelServingProvisionedThroughputStatus defines the observed
              state of ModelServingProvisionedThroughput.
            properties:
              atProvider:
                properties:
                  aiGateway:
                    description: 'A block with AI Gateway configuration for the serving
                      endpoint. Note: only external model endpoints are supported
                      as of now.'
                    properties:
                      fallbackConfig:
                        description: The model serving endpoint configuration.
                        properties:
                          enabled:
                            description: boolean flag specifying if usage tracking
                              is enabled.
                            type: boolean
                        type: object
                      guardrails:
                        description: 'Block with configuration for AI Guardrails to
                          prevent unwanted data and unsafe data in requests and responses.
                          Consists of the following attributes:'
                        properties:
                          input:
                            description: 'A block with configuration for input guardrail
                              filters:'
                            properties:
                              invalidKeywords:
                                description: List of invalid keywords. AI guardrail
                                  uses keyword or string matching to decide if the
                                  keyword exists in the request or response content.
                                items:
                                  type: string
                                type: array
                              pii:
                                description: 'Block with configuration for guardrail
                                  PII filter:'
                                properties:
                                  behavior:
                                    description: a string that describes the behavior
                                      for PII filter. Currently only BLOCK value is
                                      supported.
                                    type: string
                                type: object
                              safety:
                                description: the boolean flag that indicates whether
                                  the safety filter is enabled.
                                type: boolean
                              validTopics:
                                description: The list of allowed topics. Given a chat
                                  request, this guardrail flags the request if its
                                  topic is not in the allowed topics.
                                items:
                                  type: string
                                type: array
                            type: object
                          output:
                            description: A block with configuration for output guardrail
                              filters.  Has the same structure as input block.
                            properties:
                              invalidKeywords:
                                description: List of invalid keywords. AI guardrail
                                  uses keyword or string matching to decide if the
                                  keyword exists in the request or response content.
                                items:
                                  type: string
                                type: array
                              pii:
                                description: 'Block with configuration for guardrail
                                  PII filter:'
                                properties:
                                  behavior:
                                    description: a string that describes the behavior
                                      for PII filter. Currently only BLOCK value is
                                      supported.
                                    type: string
                                type: object
                              safety:
                                description: the boolean flag that indicates whether
                                  the safety filter is enabled.
                                type: boolean
                              validTopics:
                                description: The list of allowed topics. Given a chat
                                  request, this guardrail flags the request if its
                                  topic is not in the allowed topics.
                                items:
                                  type: string
                                type: array
                            type: object
                        type: object
                      inferenceTableConfig:
                        description: 'Block describing the configuration of usage
                          tracking. Consists of the following attributes:'
                        properties:
                          catalogName:
                            description: 'The name of the model serving endpoint.
                              This field is required and must be unique across a workspace.
                              An endpoint name can consist of alphanumeric characters,
                              dashes, and underscores. NOTE: Changing this name will
                              delete the existing endpoint and create a new endpoint
                              with the updated name.'
                            type: string
                          enabled:
                            description: boolean flag specifying if usage tracking
                              is enabled.
                            type: boolean
                          schemaName:
                            description: 'The name of the model serving endpoint.
                              This field is required and must be unique across a workspace.
                              An endpoint name can consist of alphanumeric characters,
                              dashes, and underscores. NOTE: Changing this name will
                              delete the existing endpoint and create a new endpoint
                              with the updated name.'
                            type: string
                          tableNamePrefix:
                            type: string
                        type: object
                      rateLimits:
                        description: Block describing rate limits for AI gateway.
                          For details see the description of rate_limits block above.
                        items:
                          properties:
                            calls:
                              type: number
                            key:
                              description: The key field for a tag.
                              type: string
                            principal:
                              type: string
                            renewalPeriod:
                              type: string
                            tokens:
                              type: number
                          type: object
                        type: array
                      usageTrackingConfig:
                        description: Block with configuration for payload logging
                          using inference tables. For details see the description
                          of auto_capture_config block above.
                        properties:
                          enabled:
                            description: boolean flag specifying if usage tracking
                              is enabled.
                            type: boolean
                        type: object
                    type: object
                  budgetPolicyId:
                    description: (Optiona) The Budget Policy ID set for this serving
                      endpoint.
                    type: string
                  config:
                    description: The model serving endpoint configuration.
                    properties:
                      servedEntities:
                        description: A list of served entities for the endpoint to
                          serve.
                        items:
                          properties:
                            burstScalingEnabled:
                              description: boolean flag specifying if usage tracking
                                is enabled.
                              type: boolean
                            entityName:
                              description: The full path of the UC model to be served,
                                given in the form of catalog_name.schema_name.model_name.
                              type: string
                            entityVersion:
                              description: The version of the model in UC to be served.
                              type: string
                            name:
                              description: The name of a served entity. It must be
                                unique across an endpoint. A served entity name can
                                consist of alphanumeric characters, dashes, and underscores.
                                If not specified for an external model, this field
                                will be created from the entity_name and entity_version
                              type: string
                            provisionedModelUnits:
                              description: The number of model units to be provisioned.
                              type: number
                          type: object
                        type: array
                      trafficConfig:
                        description: A single block represents the traffic split configuration
                          amongst the served models.
                        properties:
                          routes:
                            description: Each block represents a route that defines
                              traffic to each served entity. Each served_entity block
                              needs to have a corresponding routes block.
                            items:
                              properties:
                                servedEntityName:
                                  description: The name of the served entity this
                                    route configures traffic for. This needs to match
                                    the name of a served_entity block.
                                  type: string
                                servedModelName:
                                  description: 'The name of the model serving endpoint.
                                    This field is required and must be unique across
                                    a workspace. An endpoint name can consist of alphanumeric
                                    characters, dashes, and underscores. NOTE: Changing
                                    this name will delete the existing endpoint and
                                    create a new endpoint with the updated name.'
                                  type: string
                                trafficPercentage:
                                  description: The percentage of endpoint traffic
                                    to send to this route. It must be an integer between
                                    0 and 100 inclusive.
                                  type: number
                              type: object
                            type: array
                        type: object
                    type: object
                  emailNotifications:
                    description: A block with Email notification setting.
                    properties:
                      onUpdateFailure:
                        description: a list of email addresses to be notified when
                          an endpoint fails to update its configuration or state.
                        items:
                          type: string
                        type: array
                      onUpdateSuccess:
                        description: a list of email addresses to be notified when
                          an endpoint successfully updates its configuration or state.
                        items:
                          type: string
                        type: array
                    type: object
                  id:
                    description: Equal to the name argument and used to identify the
                      serving endpoint.
                    type: string
                  name:
                    description: 'The name of the model serving endpoint. This field
                      is required and must be unique across a workspace. An endpoint
                      name can consist of alphanumeric characters, dashes, and underscores.
                      NOTE: Changing this name will delete the existing endpoint and
                      create a new endpoint with the updated name.'
                    type: string
                  providerConfig:
                    description: 'Configure the provider for management through account
                      provider. This block consists of the following fields:'
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  servingEndpointId:
                    description: Unique identifier of the serving endpoint primarily
                      used to set permissions and refer to this instance for other
                      operations.
                    type: string
                  tags:
                    description: Tags to be attached to the serving endpoint and automatically
                      propagated to billing logs.
                    items:
                      properties:
                        key:
                          description: The key field for a tag.
                          type: string
                        value:
                          description: The value field for a tag.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
