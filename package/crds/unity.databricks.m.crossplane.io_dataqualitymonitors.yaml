---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: dataqualitymonitors.unity.databricks.m.crossplane.io
spec:
  group: unity.databricks.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - databricks
    kind: DataQualityMonitor
    listKind: DataQualityMonitorList
    plural: dataqualitymonitors
    singular: dataqualitymonitor
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: DataQualityMonitor is the Schema for the DataQualityMonitors
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataQualityMonitorSpec defines the desired state of DataQualityMonitor
            properties:
              forProvider:
                properties:
                  anomalyDetectionConfig:
                    description: Anomaly Detection Configuration, applicable to schema
                      object types
                    properties:
                      excludedTableFullNames:
                        description: List of fully qualified table names to exclude
                          from anomaly detection
                        items:
                          type: string
                        type: array
                    type: object
                  dataProfilingConfig:
                    description: |-
                      Data Profiling Configuration, applicable to table object types. Exactly one Analysis Configuration
                      must be present
                    properties:
                      assetsDir:
                        description: |-
                          Field for specifying the absolute path to a custom directory to store data-monitoring
                          assets. Normally prepopulated to a default user location via UI and Python APIs
                        type: string
                      baselineTableName:
                        description: |-
                          Baseline table name.
                          Baseline data is used to compute drift from the data in the monitored table_name.
                          The baseline table and the monitored table shall have the same schema
                        type: string
                      customMetrics:
                        description: Custom metrics
                        items:
                          properties:
                            definition:
                              description: Jinja template for a SQL expression that
                                specifies how to compute the metric. See create metric
                                definition
                              type: string
                            inputColumns:
                              description: |-
                                A list of column names in the input table the metric should be computed for.
                                Can use ":table" to indicate that the metric needs information from multiple columns
                              items:
                                type: string
                              type: array
                            name:
                              description: Name of the metric in the output tables
                              type: string
                            outputDataType:
                              description: The output type of the custom metric
                              type: string
                            type:
                              description: 'The type of the custom metric. Possible
                                values are: DATA_PROFILING_CUSTOM_METRIC_TYPE_AGGREGATE,
                                DATA_PROFILING_CUSTOM_METRIC_TYPE_DERIVED, DATA_PROFILING_CUSTOM_METRIC_TYPE_DRIFT'
                              type: string
                          type: object
                        type: array
                      inferenceLog:
                        description: Analysis Configuration for monitoring inference
                          log tables
                        properties:
                          granularities:
                            description: List of granularities to use when aggregating
                              data into time windows based on their timestamp
                            items:
                              type: string
                            type: array
                          labelColumn:
                            description: Column for the label
                            type: string
                          modelIdColumn:
                            description: Column for the model identifier
                            type: string
                          predictionColumn:
                            description: Column for the prediction
                            type: string
                          problemType:
                            description: 'Problem type the model aims to solve. Possible
                              values are: INFERENCE_PROBLEM_TYPE_CLASSIFICATION, INFERENCE_PROBLEM_TYPE_REGRESSION'
                            type: string
                          timestampColumn:
                            description: Column for the timestamp
                            type: string
                        type: object
                      notificationSettings:
                        description: Field for specifying notification settings
                        properties:
                          onFailure:
                            description: Destinations to send notifications on failure/timeout
                            properties:
                              emailAddresses:
                                description: The list of email addresses to send the
                                  notification to. A maximum of 5 email addresses
                                  is supported
                                items:
                                  type: string
                                type: array
                            type: object
                        type: object
                      outputSchemaId:
                        description: ID of the schema where output tables are created
                        type: string
                      schedule:
                        description: The cron schedule
                        properties:
                          quartzCronExpression:
                            description: The expression that determines when to run
                              the monitor. See examples
                            type: string
                          timezoneId:
                            description: |-
                              A Java timezone id. The schedule for a job will be resolved with respect to this timezone.
                              See Java TimeZone <http://docs.oracle.com/javase/7/docs/api/java/util/TimeZone.html>_ for details.
                              The timezone id (e.g., America/Los_Angeles) in which to evaluate the quartz expression
                            type: string
                        type: object
                      skipBuiltinDashboard:
                        description: Whether to skip creating a default dashboard
                          summarizing data quality metrics
                        type: boolean
                      slicingExprs:
                        description: |-
                          List of column expressions to slice data with for targeted analysis. The data is grouped by
                          each expression independently, resulting in a separate slice for each predicate and its
                          complements. For example slicing_exprs=[“col_1”, “col_2 > 10”] will generate the following
                          slices: two slices for col_2 > 10 (True and False), and one slice per unique value in
                          col1. For high-cardinality columns, only the top 100 unique values by frequency will
                          generate slices
                        items:
                          type: string
                        type: array
                      snapshot:
                        description: Analysis Configuration for monitoring snapshot
                          tables
                        type: object
                      timeSeries:
                        description: Analysis Configuration for monitoring time series
                          tables
                        properties:
                          granularities:
                            description: List of granularities to use when aggregating
                              data into time windows based on their timestamp
                            items:
                              type: string
                            type: array
                          timestampColumn:
                            description: Column for the timestamp
                            type: string
                        type: object
                      warehouseId:
                        description: |-
                          Optional argument to specify the warehouse for dashboard creation. If not specified, the first running
                          warehouse will be used
                        type: string
                    type: object
                  objectId:
                    description: The UUID of the request object. It is schema_id for
                      schema, and table_id for table.
                    type: string
                  objectIdRef:
                    description: Reference to a Schema in unity to populate objectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  objectIdSelector:
                    description: Selector for a Schema in unity to populate objectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  objectType:
                    description: 'The type of the monitored object. Can be one of
                      the following: schema or table'
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  anomalyDetectionConfig:
                    description: Anomaly Detection Configuration, applicable to schema
                      object types
                    properties:
                      excludedTableFullNames:
                        description: List of fully qualified table names to exclude
                          from anomaly detection
                        items:
                          type: string
                        type: array
                    type: object
                  dataProfilingConfig:
                    description: |-
                      Data Profiling Configuration, applicable to table object types. Exactly one Analysis Configuration
                      must be present
                    properties:
                      assetsDir:
                        description: |-
                          Field for specifying the absolute path to a custom directory to store data-monitoring
                          assets. Normally prepopulated to a default user location via UI and Python APIs
                        type: string
                      baselineTableName:
                        description: |-
                          Baseline table name.
                          Baseline data is used to compute drift from the data in the monitored table_name.
                          The baseline table and the monitored table shall have the same schema
                        type: string
                      customMetrics:
                        description: Custom metrics
                        items:
                          properties:
                            definition:
                              description: Jinja template for a SQL expression that
                                specifies how to compute the metric. See create metric
                                definition
                              type: string
                            inputColumns:
                              description: |-
                                A list of column names in the input table the metric should be computed for.
                                Can use ":table" to indicate that the metric needs information from multiple columns
                              items:
                                type: string
                              type: array
                            name:
                              description: Name of the metric in the output tables
                              type: string
                            outputDataType:
                              description: The output type of the custom metric
                              type: string
                            type:
                              description: 'The type of the custom metric. Possible
                                values are: DATA_PROFILING_CUSTOM_METRIC_TYPE_AGGREGATE,
                                DATA_PROFILING_CUSTOM_METRIC_TYPE_DERIVED, DATA_PROFILING_CUSTOM_METRIC_TYPE_DRIFT'
                              type: string
                          type: object
                        type: array
                      inferenceLog:
                        description: Analysis Configuration for monitoring inference
                          log tables
                        properties:
                          granularities:
                            description: List of granularities to use when aggregating
                              data into time windows based on their timestamp
                            items:
                              type: string
                            type: array
                          labelColumn:
                            description: Column for the label
                            type: string
                          modelIdColumn:
                            description: Column for the model identifier
                            type: string
                          predictionColumn:
                            description: Column for the prediction
                            type: string
                          problemType:
                            description: 'Problem type the model aims to solve. Possible
                              values are: INFERENCE_PROBLEM_TYPE_CLASSIFICATION, INFERENCE_PROBLEM_TYPE_REGRESSION'
                            type: string
                          timestampColumn:
                            description: Column for the timestamp
                            type: string
                        type: object
                      notificationSettings:
                        description: Field for specifying notification settings
                        properties:
                          onFailure:
                            description: Destinations to send notifications on failure/timeout
                            properties:
                              emailAddresses:
                                description: The list of email addresses to send the
                                  notification to. A maximum of 5 email addresses
                                  is supported
                                items:
                                  type: string
                                type: array
                            type: object
                        type: object
                      outputSchemaId:
                        description: ID of the schema where output tables are created
                        type: string
                      schedule:
                        description: The cron schedule
                        properties:
                          quartzCronExpression:
                            description: The expression that determines when to run
                              the monitor. See examples
                            type: string
                          timezoneId:
                            description: |-
                              A Java timezone id. The schedule for a job will be resolved with respect to this timezone.
                              See Java TimeZone <http://docs.oracle.com/javase/7/docs/api/java/util/TimeZone.html>_ for details.
                              The timezone id (e.g., America/Los_Angeles) in which to evaluate the quartz expression
                            type: string
                        type: object
                      skipBuiltinDashboard:
                        description: Whether to skip creating a default dashboard
                          summarizing data quality metrics
                        type: boolean
                      slicingExprs:
                        description: |-
                          List of column expressions to slice data with for targeted analysis. The data is grouped by
                          each expression independently, resulting in a separate slice for each predicate and its
                          complements. For example slicing_exprs=[“col_1”, “col_2 > 10”] will generate the following
                          slices: two slices for col_2 > 10 (True and False), and one slice per unique value in
                          col1. For high-cardinality columns, only the top 100 unique values by frequency will
                          generate slices
                        items:
                          type: string
                        type: array
                      snapshot:
                        description: Analysis Configuration for monitoring snapshot
                          tables
                        type: object
                      timeSeries:
                        description: Analysis Configuration for monitoring time series
                          tables
                        properties:
                          granularities:
                            description: List of granularities to use when aggregating
                              data into time windows based on their timestamp
                            items:
                              type: string
                            type: array
                          timestampColumn:
                            description: Column for the timestamp
                            type: string
                        type: object
                      warehouseId:
                        description: |-
                          Optional argument to specify the warehouse for dashboard creation. If not specified, the first running
                          warehouse will be used
                        type: string
                    type: object
                  objectId:
                    description: The UUID of the request object. It is schema_id for
                      schema, and table_id for table.
                    type: string
                  objectIdRef:
                    description: Reference to a Schema in unity to populate objectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  objectIdSelector:
                    description: Selector for a Schema in unity to populate objectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  objectType:
                    description: 'The type of the monitored object. Can be one of
                      the following: schema or table'
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.objectType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.objectType)
                || (has(self.initProvider) && has(self.initProvider.objectType))'
          status:
            description: DataQualityMonitorStatus defines the observed state of DataQualityMonitor.
            properties:
              atProvider:
                properties:
                  anomalyDetectionConfig:
                    description: Anomaly Detection Configuration, applicable to schema
                      object types
                    properties:
                      excludedTableFullNames:
                        description: List of fully qualified table names to exclude
                          from anomaly detection
                        items:
                          type: string
                        type: array
                    type: object
                  dataProfilingConfig:
                    description: |-
                      Data Profiling Configuration, applicable to table object types. Exactly one Analysis Configuration
                      must be present
                    properties:
                      assetsDir:
                        description: |-
                          Field for specifying the absolute path to a custom directory to store data-monitoring
                          assets. Normally prepopulated to a default user location via UI and Python APIs
                        type: string
                      baselineTableName:
                        description: |-
                          Baseline table name.
                          Baseline data is used to compute drift from the data in the monitored table_name.
                          The baseline table and the monitored table shall have the same schema
                        type: string
                      customMetrics:
                        description: Custom metrics
                        items:
                          properties:
                            definition:
                              description: Jinja template for a SQL expression that
                                specifies how to compute the metric. See create metric
                                definition
                              type: string
                            inputColumns:
                              description: |-
                                A list of column names in the input table the metric should be computed for.
                                Can use ":table" to indicate that the metric needs information from multiple columns
                              items:
                                type: string
                              type: array
                            name:
                              description: Name of the metric in the output tables
                              type: string
                            outputDataType:
                              description: The output type of the custom metric
                              type: string
                            type:
                              description: 'The type of the custom metric. Possible
                                values are: DATA_PROFILING_CUSTOM_METRIC_TYPE_AGGREGATE,
                                DATA_PROFILING_CUSTOM_METRIC_TYPE_DERIVED, DATA_PROFILING_CUSTOM_METRIC_TYPE_DRIFT'
                              type: string
                          type: object
                        type: array
                      dashboardId:
                        type: string
                      driftMetricsTableName:
                        description: Name of the metric in the output tables
                        type: string
                      effectiveWarehouseId:
                        description: |-
                          Optional argument to specify the warehouse for dashboard creation. If not specified, the first running
                          warehouse will be used
                        type: string
                      inferenceLog:
                        description: Analysis Configuration for monitoring inference
                          log tables
                        properties:
                          granularities:
                            description: List of granularities to use when aggregating
                              data into time windows based on their timestamp
                            items:
                              type: string
                            type: array
                          labelColumn:
                            description: Column for the label
                            type: string
                          modelIdColumn:
                            description: Column for the model identifier
                            type: string
                          predictionColumn:
                            description: Column for the prediction
                            type: string
                          problemType:
                            description: 'Problem type the model aims to solve. Possible
                              values are: INFERENCE_PROBLEM_TYPE_CLASSIFICATION, INFERENCE_PROBLEM_TYPE_REGRESSION'
                            type: string
                          timestampColumn:
                            description: Column for the timestamp
                            type: string
                        type: object
                      latestMonitorFailureMessage:
                        type: string
                      monitorVersion:
                        type: number
                      monitoredTableName:
                        description: Name of the metric in the output tables
                        type: string
                      notificationSettings:
                        description: Field for specifying notification settings
                        properties:
                          onFailure:
                            description: Destinations to send notifications on failure/timeout
                            properties:
                              emailAddresses:
                                description: The list of email addresses to send the
                                  notification to. A maximum of 5 email addresses
                                  is supported
                                items:
                                  type: string
                                type: array
                            type: object
                        type: object
                      outputSchemaId:
                        description: ID of the schema where output tables are created
                        type: string
                      profileMetricsTableName:
                        description: Name of the metric in the output tables
                        type: string
                      schedule:
                        description: The cron schedule
                        properties:
                          pauseStatus:
                            type: string
                          quartzCronExpression:
                            description: The expression that determines when to run
                              the monitor. See examples
                            type: string
                          timezoneId:
                            description: |-
                              A Java timezone id. The schedule for a job will be resolved with respect to this timezone.
                              See Java TimeZone <http://docs.oracle.com/javase/7/docs/api/java/util/TimeZone.html>_ for details.
                              The timezone id (e.g., America/Los_Angeles) in which to evaluate the quartz expression
                            type: string
                        type: object
                      skipBuiltinDashboard:
                        description: Whether to skip creating a default dashboard
                          summarizing data quality metrics
                        type: boolean
                      slicingExprs:
                        description: |-
                          List of column expressions to slice data with for targeted analysis. The data is grouped by
                          each expression independently, resulting in a separate slice for each predicate and its
                          complements. For example slicing_exprs=[“col_1”, “col_2 > 10”] will generate the following
                          slices: two slices for col_2 > 10 (True and False), and one slice per unique value in
                          col1. For high-cardinality columns, only the top 100 unique values by frequency will
                          generate slices
                        items:
                          type: string
                        type: array
                      snapshot:
                        description: Analysis Configuration for monitoring snapshot
                          tables
                        type: object
                      status:
                        type: string
                      timeSeries:
                        description: Analysis Configuration for monitoring time series
                          tables
                        properties:
                          granularities:
                            description: List of granularities to use when aggregating
                              data into time windows based on their timestamp
                            items:
                              type: string
                            type: array
                          timestampColumn:
                            description: Column for the timestamp
                            type: string
                        type: object
                      warehouseId:
                        description: |-
                          Optional argument to specify the warehouse for dashboard creation. If not specified, the first running
                          warehouse will be used
                        type: string
                    type: object
                  id:
                    type: string
                  objectId:
                    description: The UUID of the request object. It is schema_id for
                      schema, and table_id for table.
                    type: string
                  objectType:
                    description: 'The type of the monitored object. Can be one of
                      the following: schema or table'
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
