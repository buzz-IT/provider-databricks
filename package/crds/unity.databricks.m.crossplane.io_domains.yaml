---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: domains.unity.databricks.m.crossplane.io
spec:
  group: unity.databricks.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - databricks
    kind: Domain
    listKind: DomainList
    plural: domains
    singular: domain
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Domain is the Schema for the Domains API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DomainSpec defines the desired state of Domain
            properties:
              forProvider:
                properties:
                  businessOwnerIds:
                    description: Principal IDs of the business owners (users, groups,
                      or service principals)
                    items:
                      type: number
                    type: array
                  description:
                    description: Full description (max 4096 chars)
                    type: string
                  domainId:
                    description: |-
                      Unique identifier for the domain. If omitted at Create, the server
                      generates one
                    type: string
                  draft:
                    description: |-
                      Whether to mark the domain as a draft. If omitted on Create, the server
                      applies a default; the resolved value is returned in effective_draft
                    type: boolean
                  icon:
                    description: Icon to display for the domain
                    properties:
                      color:
                        description: 'Hex color code with # prefix (e.g., "#FF5733")'
                        type: string
                      name:
                        description: 'Possible values are: ADDRESS_BOOK, ALARM, ARROWS_IN,
                          ATOM, BALLOON, BANK, BARRICADE, BASKET, BRIDGE, CACTUS,
                          CALL_BELL, CARROT, CHART_PIE_SLICE, CITY, CLOUD, COINS,
                          COMPASS_ROSE, CRANE_TOWER, CROWN, CUBE_TRANSPARENT, FADERS,
                          FLAG_BANNER_FOLD, FLAG_CHECKERED, GAVEL, HAMBURGER, HEAD_CIRCUIT,
                          HOURGLASS_HIGH, INTERSECT_THREE, MICROSCOPE, MOON_STARS,
                          PACKAGE, PARACHUTE, PEPPER, PIGGY_BANK, PILL, PLANET, PLANT,
                          PLUGS_CONNECTED, POPCORN, PRESENTATION_CHART, PUZZLE_PIECE,
                          RAINBOW, RANKING, RECEIPT, ROCKET, RULER, SAILBOAT, SCALES,
                          SCAN_SMILEY, SCROLL, SHIELD_CHECKERED, SNEAKER, SNOWFLAKE,
                          SOLAR_ROOF, SPEEDOMETER, STAMP, STEPS, STRATEGY, SWORD,
                          TELEVISION_SIMPLE, TENT, TICKET, TRACTOR, TRAFFIC_CONE,
                          TRAIN, TREE_EVERGREEN, TREE_STRUCTURE, TROLLEY_SUITCASE,
                          TROPHY, TRUCK_TRAILER, USERS_THREE, VECTOR_THREE'
                        type: string
                    type: object
                  parentDomainId:
                    description: |-
                      Domain ID of the parent. If absent, this is a top-level domain.
                      If present, this domain is a subdomain of the specified parent
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  subtitle:
                    description: Short description (max 280 chars)
                    type: string
                  tagKey:
                    description: Governed tag key associated with this domain
                    type: string
                  technicalOwnerIds:
                    description: Principal IDs of the technical owners (users, groups,
                      or service principals)
                    items:
                      type: number
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  businessOwnerIds:
                    description: Principal IDs of the business owners (users, groups,
                      or service principals)
                    items:
                      type: number
                    type: array
                  description:
                    description: Full description (max 4096 chars)
                    type: string
                  domainId:
                    description: |-
                      Unique identifier for the domain. If omitted at Create, the server
                      generates one
                    type: string
                  draft:
                    description: |-
                      Whether to mark the domain as a draft. If omitted on Create, the server
                      applies a default; the resolved value is returned in effective_draft
                    type: boolean
                  icon:
                    description: Icon to display for the domain
                    properties:
                      color:
                        description: 'Hex color code with # prefix (e.g., "#FF5733")'
                        type: string
                      name:
                        description: 'Possible values are: ADDRESS_BOOK, ALARM, ARROWS_IN,
                          ATOM, BALLOON, BANK, BARRICADE, BASKET, BRIDGE, CACTUS,
                          CALL_BELL, CARROT, CHART_PIE_SLICE, CITY, CLOUD, COINS,
                          COMPASS_ROSE, CRANE_TOWER, CROWN, CUBE_TRANSPARENT, FADERS,
                          FLAG_BANNER_FOLD, FLAG_CHECKERED, GAVEL, HAMBURGER, HEAD_CIRCUIT,
                          HOURGLASS_HIGH, INTERSECT_THREE, MICROSCOPE, MOON_STARS,
                          PACKAGE, PARACHUTE, PEPPER, PIGGY_BANK, PILL, PLANET, PLANT,
                          PLUGS_CONNECTED, POPCORN, PRESENTATION_CHART, PUZZLE_PIECE,
                          RAINBOW, RANKING, RECEIPT, ROCKET, RULER, SAILBOAT, SCALES,
                          SCAN_SMILEY, SCROLL, SHIELD_CHECKERED, SNEAKER, SNOWFLAKE,
                          SOLAR_ROOF, SPEEDOMETER, STAMP, STEPS, STRATEGY, SWORD,
                          TELEVISION_SIMPLE, TENT, TICKET, TRACTOR, TRAFFIC_CONE,
                          TRAIN, TREE_EVERGREEN, TREE_STRUCTURE, TROLLEY_SUITCASE,
                          TROPHY, TRUCK_TRAILER, USERS_THREE, VECTOR_THREE'
                        type: string
                    type: object
                  parentDomainId:
                    description: |-
                      Domain ID of the parent. If absent, this is a top-level domain.
                      If present, this domain is a subdomain of the specified parent
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  subtitle:
                    description: Short description (max 280 chars)
                    type: string
                  tagKey:
                    description: Governed tag key associated with this domain
                    type: string
                  technicalOwnerIds:
                    description: Principal IDs of the technical owners (users, groups,
                      or service principals)
                    items:
                      type: number
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.tagKey is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.tagKey)
                || (has(self.initProvider) && has(self.initProvider.tagKey))'
          status:
            description: DomainStatus defines the observed state of Domain.
            properties:
              atProvider:
                properties:
                  businessOwnerIds:
                    description: Principal IDs of the business owners (users, groups,
                      or service principals)
                    items:
                      type: number
                    type: array
                  createTime:
                    description: Timestamp when the domain was created
                    type: string
                  description:
                    description: Full description (max 4096 chars)
                    type: string
                  domainId:
                    description: |-
                      Unique identifier for the domain. If omitted at Create, the server
                      generates one
                    type: string
                  draft:
                    description: |-
                      Whether to mark the domain as a draft. If omitted on Create, the server
                      applies a default; the resolved value is returned in effective_draft
                    type: boolean
                  effectiveDraft:
                    description: Resolved draft state of the domain
                    type: boolean
                  icon:
                    description: Icon to display for the domain
                    properties:
                      color:
                        description: 'Hex color code with # prefix (e.g., "#FF5733")'
                        type: string
                      name:
                        description: 'Possible values are: ADDRESS_BOOK, ALARM, ARROWS_IN,
                          ATOM, BALLOON, BANK, BARRICADE, BASKET, BRIDGE, CACTUS,
                          CALL_BELL, CARROT, CHART_PIE_SLICE, CITY, CLOUD, COINS,
                          COMPASS_ROSE, CRANE_TOWER, CROWN, CUBE_TRANSPARENT, FADERS,
                          FLAG_BANNER_FOLD, FLAG_CHECKERED, GAVEL, HAMBURGER, HEAD_CIRCUIT,
                          HOURGLASS_HIGH, INTERSECT_THREE, MICROSCOPE, MOON_STARS,
                          PACKAGE, PARACHUTE, PEPPER, PIGGY_BANK, PILL, PLANET, PLANT,
                          PLUGS_CONNECTED, POPCORN, PRESENTATION_CHART, PUZZLE_PIECE,
                          RAINBOW, RANKING, RECEIPT, ROCKET, RULER, SAILBOAT, SCALES,
                          SCAN_SMILEY, SCROLL, SHIELD_CHECKERED, SNEAKER, SNOWFLAKE,
                          SOLAR_ROOF, SPEEDOMETER, STAMP, STEPS, STRATEGY, SWORD,
                          TELEVISION_SIMPLE, TENT, TICKET, TRACTOR, TRAFFIC_CONE,
                          TRAIN, TREE_EVERGREEN, TREE_STRUCTURE, TROLLEY_SUITCASE,
                          TROPHY, TRUCK_TRAILER, USERS_THREE, VECTOR_THREE'
                        type: string
                    type: object
                  id:
                    type: string
                  name:
                    description: 'Possible values are: ADDRESS_BOOK, ALARM, ARROWS_IN,
                      ATOM, BALLOON, BANK, BARRICADE, BASKET, BRIDGE, CACTUS, CALL_BELL,
                      CARROT, CHART_PIE_SLICE, CITY, CLOUD, COINS, COMPASS_ROSE, CRANE_TOWER,
                      CROWN, CUBE_TRANSPARENT, FADERS, FLAG_BANNER_FOLD, FLAG_CHECKERED,
                      GAVEL, HAMBURGER, HEAD_CIRCUIT, HOURGLASS_HIGH, INTERSECT_THREE,
                      MICROSCOPE, MOON_STARS, PACKAGE, PARACHUTE, PEPPER, PIGGY_BANK,
                      PILL, PLANET, PLANT, PLUGS_CONNECTED, POPCORN, PRESENTATION_CHART,
                      PUZZLE_PIECE, RAINBOW, RANKING, RECEIPT, ROCKET, RULER, SAILBOAT,
                      SCALES, SCAN_SMILEY, SCROLL, SHIELD_CHECKERED, SNEAKER, SNOWFLAKE,
                      SOLAR_ROOF, SPEEDOMETER, STAMP, STEPS, STRATEGY, SWORD, TELEVISION_SIMPLE,
                      TENT, TICKET, TRACTOR, TRAFFIC_CONE, TRAIN, TREE_EVERGREEN,
                      TREE_STRUCTURE, TROLLEY_SUITCASE, TROPHY, TRUCK_TRAILER, USERS_THREE,
                      VECTOR_THREE'
                    type: string
                  parentDomainId:
                    description: |-
                      Domain ID of the parent. If absent, this is a top-level domain.
                      If present, this domain is a subdomain of the specified parent
                    type: string
                  providerConfig:
                    description: Configure the provider for management through account
                      provider.
                    properties:
                      workspaceId:
                        description: Workspace ID which the resource belongs to. This
                          workspace must be part of the account which the provider
                          is configured with.
                        type: string
                    type: object
                  subtitle:
                    description: Short description (max 280 chars)
                    type: string
                  tagKey:
                    description: Governed tag key associated with this domain
                    type: string
                  technicalOwnerIds:
                    description: Principal IDs of the technical owners (users, groups,
                      or service principals)
                    items:
                      type: number
                    type: array
                  updateTime:
                    description: Timestamp when the domain was last updated
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
